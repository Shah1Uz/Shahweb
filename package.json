{
  "name": "portfolio-cms-monorepo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix server run build",
    "build": "npm --prefix server install && npm --prefix server run build && npm --prefix server run prisma:push",
    "start": "npm --prefix server start",
    "dev": "npm --prefix server start"
  }
}
