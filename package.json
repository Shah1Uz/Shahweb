{
  "name": "portfolio-cms-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix server install && npm --prefix server run build && npx --prefix server prisma db push",
    "start": "npm --prefix server start",
    "dev": "npm --prefix server start"
  }
}
